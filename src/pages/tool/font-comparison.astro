<html lang="bn">
  <head>
    <title>Font Comparison Tool</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 font-sans min-h-screen py-10 px-4">
    <main class="max-w-6xl mx-auto">
      <h1 class="text-4xl font-bold mb-8 text-center">Font Comparison Tool</h1>

      <!-- Sample text input -->
      <section class="mb-8">
        <label for="sampleText" class="block text-lg font-medium mb-2">Sample Text</label>
        <textarea
          id="sampleText"
          rows="2"
          class="w-full p-4 text-lg border border-gray-300 dark:border-gray-700 rounded-lg shadow-sm bg-white dark:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500 dark:focus:ring-blue-400"
        >বাংলা ভাষার সৌন্দর্য ফুটে ওঠে এই ফন্টে।</textarea>
      </section>

      <!-- Font selection -->
      <section class="grid grid-cols-1 sm:grid-cols-2 gap-6 mb-10">
        <div>
          <label for="fontA" class="block text-lg font-medium mb-2">Font A</label>
          <select
            id="fontA"
            class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg bg-white dark:bg-gray-800 shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 dark:focus:ring-blue-400"
          ></select>
        </div>
        <div>
          <label for="fontB" class="block text-lg font-medium mb-2">Font B</label>
          <select
            id="fontB"
            class="w-full p-3 border border-gray-300 dark:border-gray-700 rounded-lg bg-white dark:bg-gray-800 shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 dark:focus:ring-blue-400"
          ></select>
        </div>
      </section>

      <!-- Font comparison display -->
      <section id="comparison" class="grid grid-cols-1 md:grid-cols-2 gap-8"></section>
    </main>

    <!-- External JS -->
    <script src="/assets/tools/font-comparison.js" defer></script>
  </body>
</html>
